# MT5 AutoTrader V3 FINAL

V3 fixes the previous runtime issue and precomputes indicators/signals once per strategy rather than recalculating them on every historical candle.

Six strategies plus Ensemble are evaluated with train/test separation. Reports include strategy summaries, symbol summaries, trades and monthly summaries.

Research only — no orders are sent.
